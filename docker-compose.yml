version: "3"

services
  webserver:
    build: .
    dockerfile: Dockerfile
    container_name: ${WEBSERVER_NAME}
    volumes:
        - ./sites:/sites
    ports:
        - "80:80"
    restart: always
